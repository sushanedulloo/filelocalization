You are extracting structured symptom signals from a software issue report.

ISSUE:
{issue}

Return ONLY a JSON object exactly of this shape — leave any field as an empty list if it does not apply:
{{
  "exception_types": ["KeyError", "TypeError", ...],
  "error_messages":  ["literal substrings quoted in the issue"],
  "stack_frames":    [{{"file": "path/to/file.py", "func": "name", "line": 42}}],
  "behaviors":       ["short noun phrases describing the affected behavior"],
  "api_calls_named": ["Class.method", "module.function"]
}}
