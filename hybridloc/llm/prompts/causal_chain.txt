You are diagnosing a software bug by reasoning backwards from observed symptoms to plausible root-cause functions.

ISSUE:
{issue}

OBSERVED SYMPTOMS:
{symptoms}

CURRENT CAUSAL GRAPH (Mermaid):
{cig_mermaid}

NEWLY VISITED FUNCTIONS (with code):
{candidates}

Task: For each newly visited function, construct a CAUSAL CHAIN explaining
HOW that function could plausibly produce the observed symptoms. If no
plausible chain exists for a function, say so and score it 0.

Score scale (integers 0-10):
  0  = no plausible link
  1-3 = weak / coincidental
  4-6 = possible contributor
  7-8 = likely root cause
  9-10 = clear root cause with explicit chain to the symptom

Also propose up to 5 functions worth visiting next (by callee, callsite,
inheritance, or co-evolution).

Return ONLY a JSON object:
{{
  "chains": [
    {{"function": "<file_path>::<qualname>", "chain": ["step 1", "step 2", ...], "score": 0}}
  ],
  "next_to_explore": ["<file_path>::<qualname>", ...]
}}
